import { title } from "@/components/ui/primitives";

export default function PricingPage() {
    return (
        <div>
            <h1 className={title({ color: "pink" })}>Pricing</h1>
        </div>
    );
}
